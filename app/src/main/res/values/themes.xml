<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Pure black background: on a waveguide display black is fully transparent,
         so the app frame itself disappears into the real world. -->
    <style name="Theme.SpyHunt" parent="@android:style/Theme.Material.NoActionBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
